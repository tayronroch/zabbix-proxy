version: '3.5'

# 🚀 CoreWise Zabbix Proxy + Scripts SSH
# Sistema de proxy para monitoramento distribuído com coleta SSH
# Controle de acesso via firewall

services:
  zabbix-proxy:
    build:
      context: .
      dockerfile: Dockerfile.proxy
    image: corewise-zabbix-proxy:latest
    container_name: corewise-proxy
    environment:
      ZBX_HOSTNAME: corewise-proxy
    ports:
      - "10051:10051/tcp"
    restart: always
    labels:
      - "coolify.managed=true" 